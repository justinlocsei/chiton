from chiton.wintour.weights import BaseWeight


class PantRiseWeight(BaseWeight):
    """A weight that compares user's body type with the rise of pants."""

    name = 'Pant rise'
    slug = 'pant-rise'
